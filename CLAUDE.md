# gongo-radar

## 프로젝트

지원사업 공고 레이더. 초기 창업기업 대표 본인이 주 1~2회 5분간 폰으로 쓰는 웹앱.

K-Startup은 모든 공고를 보여주지만, 이 앱은 우리 회사가 자격이 되는 것만 보여준다.

## 절대 규칙

1. 공고 본문을 AI로 분석하지 않는다. 자격 판정은 API 필드만으로 한다.
2. 판정 결과는 **충족 / 확인 필요 / 미해당** 3단계다. 앱이 "지원 가능합니다"라고 단정하는 문구를 절대 쓰지 않는다. 어느 조건이 어떻게 맞았는지만 보여준다.
3. 화면 문구는 반드시 "자격이 됐던 마감 공고"다. "놓친 공고"라고 쓰지 않는다.
4. 사용자에게 신청 여부를 입력받지 않는다. 마감 공고를 전부 보여주고 체크박스로 빼게 한다.
5. 공고 수집 단계에 지역 필터를 걸지 않는다. 전국을 수집한다. 지역 한정은 지도 화면 집계에서만 한다.
6. 서버, DB, 로그인, 알림 발송, 사업계획서 작성, 다중 회사 관리는 만들지 않는다.
7. 상태 저장은 브라우저 localStorage만 쓴다.
8. 시군구 단위 지도를 만들지 않는다. API의 supt_regin 은 시도 단위뿐이라 시군구 데이터가 존재하지 않는다. S3은 시도별 가로 막대다.
9. 지원금은 통합공고 예산 ÷ 개사 수로 계산한 추정치다. 화면에 반드시 "기업당 추정"이라고 쓰고, 상세 화면에 원문과 나눗셈 과정을 노출한다.
10. 지원금이 없으면 '미표기'라고 쓴다. 0원이라고 쓰지 않는다.
11. S2 화면은 건수를 가장 크게, 확인된 금액을 그 아래 작게 쓴다. 금액이 붙는 공고가 12%뿐이라 합계만 크게 띄우면 반박당한다.

## 데이터 사양

- 화면은 `public/data/announcements.json` 만 읽는다. API를 직접 호출하지 않는다(CORS).
- `targetCodes`, `bizAges`, `ages` 는 배열이다. 문자열로 다루면 부분 일치로 오작동한다.
- 판정 4개 필드가 전부 100% 채워져 있어 '확인필요'는 거의 나오지 않는다. 정상이다.

## S0 입력값 (v1에서 교체됨)

- 소재지: 17개 시도 단일 선택 (시군구 2단 선택 폐기)
- 업력: 예비창업 / 1년 / 2년 / 3년 / 5년 / 7년 / 10년 미만 / 10년 이상
- 신청 자격: 일반기업 / 1인 창조기업 / 개인(일반인) — '개인/법인/미등록' 은 폐기. API 값 체계와 축이 다르다. '예비창업자'도 여기 없다: 그건 aply_trgt(신청대상)가 아니라 biz_enyy(업력) 축의 값이라 신청대상에 넣으면 전건 미해당이 된다. 예비창업자는 업력 항목에서 고른다.
- 대표 연령: 39세 이하 / 40세 이상

## S1 화면 규칙

- '확인필요' 노란 태그는 화면에 만들지 않는다. 판정 4개 필드가 100% 채워져 있어 실제로 한 건도 나오지 않는다.
- 판정 로직의 3단계(충족/확인필요/미해당)는 방어 장치로 그대로 유지한다. 다만 화면에는 쓰지 않는다.

## S2 집계 규칙

- S2는 "마감 + 지원가능 + amountPerCompany 가 있는 것"만 센다. 금액이 없는 건은 건수에서도 뺀다.
- 대신 화면에 "지원금이 표기되지 않은 N건은 제외했습니다"를 반드시 명시한다.
- 이유: 금액 없는 것까지 세면 956건이 되어 "반년에 956개를 놓쳤다고?"로 앱이 의심받는다.

## 디자인 톤

- 저채도. 색은 상태 표시에만 쓴다 — 충족 초록, 확인 필요 노랑, 인접 파랑.
- 대표자 대상이라 화려한 화면은 가벼워 보인다. 여백과 숫자 크기로 위계를 만든다.
- 폰 우선. 지표 카드는 폰에서 세로로 쌓인다.

## 작업 방식

- 한 번에 한 화면씩 만든다. 시키지 않은 화면을 미리 만들지 않는다.
- 새 npm 패키지를 설치하기 전에 먼저 물어본다.
- 답변은 한국어로, 코드 설명은 짧게.
