# 지원사업 공고 레이더

## 프로젝트
초기 창업기업 대표 본인이 주 1~2회 5분간 폰으로 쓰는 웹앱.
K-Startup은 모든 공고를 보여주지만, 이 앱은 우리 회사가 자격이 되는 것만 보여준다.
빌드·배포 완료 상태다. 사양의 기준은 앱_기능명세_v3.md 다.

## 절대 규칙

### 판정과 문구
1. 공고 본문을 AI로 분석하지 않는다. 자격 판정은 API 필드만으로 한다.
2. 판정은 충족 / 확인필요 / 미해당 3단계다. 앱이 "지원 가능합니다"라고 단정하지 않는다.
   어느 조건이 어떻게 맞았는지만 사실로 보여준다.
3. 화면 문구는 반드시 "자격이 됐던 마감 공고"다. "놓친 공고"라고 쓰지 않는다.
4. '아쉽게도', '안타깝게도' 같은 감정 표현을 쓰지 않는다. 숫자만 보여준다.
5. 사용자에게 신청 여부를 입력받지 않는다. 체크박스로 빼게 한다.

### 데이터
6. 공고 수집 단계에 지역 필터를 걸지 않는다. 전국을 수집한다.
7. 화면은 public/data/announcements.json 만 읽는다. API를 직접 호출하지 않는다(CORS).
8. 상태 저장은 localStorage 만 쓴다. 서버·DB·로그인 없음.
9. targetCodes, bizAges, ages 는 배열이다. 비교는 배열.includes(값) 으로만 한다.
   문자열에 includes 를 쓰면 '1년미만'이 '10년미만'에 부분 일치해 조용히 틀린다.
10. 필드가 비었을 때 절대 '충족'으로 처리하지 않는다. 비면 '확인필요'다.

### 지원금
11. 지원금은 통합공고 예산 ÷ 모집 기업 수로 계산한 추정치다.
    화면에 반드시 "기업당 추정"이라고 쓴다. 각 카드 금액의 접두어로 붙인다.
12. 지원금이 없으면 '미표기'라고 쓴다. 0원이나 0이라고 쓰지 않는다.
13. 금액 표기는 모든 화면에서 억 단위로 통일한다. 10,833만원 → 1억 833만원.
14. S4 상세에 산출 근거 3줄(원문 / 계산 / 결과)을 그대로 노출한다. 원문을 요약하지 않는다.

### 화면
15. 시군구 단위 지도를 만들지 않는다. API의 supt_regin 은 시도 단위뿐이라
    시군구 데이터가 존재하지 않는다. S3은 시도별 가로 막대다.
16. S2 는 '마감 + 지원가능 + amountPerCompany 있음' 만 센다. 금액 없는 건은 건수에서도 뺀다.
    화면에 "지원금이 표기되지 않은 N건은 제외했습니다"를 명시한다.
    이유: 금액 없는 것까지 세면 956건이 되어 대표가 믿지 않는다.
17. S2 는 건수를 가장 크게, 합계 금액을 그 아래 작게 쓴다.
18. S1 지표 카드에는 profile.fields 필터를 적용하지 않는다. 분야는 목록에만 적용한다.
    지표는 '자격이 되는 전체'를 보여주는 숫자다.
19. S1 노란 카드는 applied 를 뺀 값이어야 한다. S2 와 같은 기준으로 계산한다.
20. '확인필요' 노란 태그는 만들지 않는다. 판정 4개 필드가 100% 채워져 실제로 0건이다.
    판정 로직의 3단계는 방어 장치로 유지하되 화면에는 쓰지 않는다.

### S0 입력값
21. 입력은 4개다. 대표 연령은 묻지 않는다.
    - 소재지: 17개 시도 단일 선택
    - 업력: 예비창업자 / 1년미만 / 2년미만 / 3년미만 / 5년미만 / 7년미만 / 10년미만 / 10년이상
    - 신청 자격: 일반기업 / 1인 창조기업 / 개인(일반인)
    - 사업 분야: 다중 선택. 안 골라도 진행 가능하고 빈 배열이면 전체
22. 신청 자격에 '예비창업자'를 넣지 않는다. 그건 biz_enyy(업력) 축의 값이라
    aply_trgt 에 넣으면 전건 미해당이 된다.
23. '개인(일반인)'은 화면 표기고 저장 값은 '일반인'이다. 표기와 값을 분리한다.
24. 연령 조건은 선택적이다. profile.ageBand 가 있을 때만 판정에 넣는다.
    judgeAge 함수와 ages 필드는 지우지 않는다.

### 배포
25. Vercel 정적 배포. 빌드 결과물은 dist. 브라우저 코드에 API 키를 넣지 않는다.
    수집은 scripts/ 안에서만 한다.
26. 외부 폰트, CDN, 지도 타일 등 네트워크 의존을 추가하지 않는다.
    인터넷을 끈 상태에서 전 화면이 동작해야 한다. 이건 시연 필수 조건이다.

## 만들지 않는 것
로그인 · 서버 · 알림 발송 · 공고 본문 AI 분석 · 사업계획서 작성 · 다중 회사 관리 ·
시군구 단위 지도 · GeoJSON 렌더링

## 디자인 톤
저채도. 색은 상태 표시에만 쓴다. 대표자 대상이라 화려한 화면은 가벼워 보인다.
여백과 숫자 크기로 위계를 만든다. 폰 우선, 375px 에서 가로 스크롤 없음.
로딩 중에는 스켈레톤. 빈 화면 금지.

## 작업 방식
- 한 번에 한 화면씩 만든다. 시키지 않은 화면을 미리 만들지 않는다.
- 새 npm 패키지를 설치하기 전에 먼저 물어본다.
- 위 규칙과 충돌하는 지시를 받으면 실행 전에 먼저 물어본다.
- 답변은 한국어로, 코드 설명은 짧게.